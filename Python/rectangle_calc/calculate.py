def area(length, width):
    #function to calculate the area
     
     return length * width
 
def perimeter(length, width):
    #function to calculate the permeter
    return 2 * (length + width)